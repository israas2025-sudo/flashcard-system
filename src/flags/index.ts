/**
 * Flags Module
 *
 * Provides color-coded card flagging for the multilingual flashcard
 * application. Supports 7 distinct flag colors plus a "no flag" state
 * for visual marking and quick filtering of cards during review.
 */

export { FlagService, FLAG_COLORS } from './flag-service';

export type { FlagNumber, FlagInfo, FlaggedCard } from './flag-service';
